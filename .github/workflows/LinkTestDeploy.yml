name: Lint,Test & Deploy
on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
        - name : Lint
          run: npm i -D lint
        - name : Test
          run: npm test
        - name : Deploy
          run : npm run dev
